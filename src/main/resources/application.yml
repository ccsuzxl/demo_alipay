spring:
  datasource:
    name: test
    url: jdbc:mysql://localhost:3306/zxl #url
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  mvc:
    view:
      prefix: /
      suffix: .jsp

#自定义的值，通过@Value("${a}")获取
a: dd

